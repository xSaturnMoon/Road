name: Bloom
options:
  bundleIdPrefix: com.xsaturnmoon.bloom
targets:
  Bloom:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources: [Sources]
    settings:
      INFOPLIST_FILE: Sources/Info.plist
      PRODUCT_BUNDLE_IDENTIFIER: com.xsaturnmoon.bloom
      ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
